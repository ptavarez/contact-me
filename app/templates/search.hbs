<h3>Search Results</h3>

{{#if model}}

{{#each model as |contact|}}

{{#link-to 'contact' contact}}
<div class='col-lg-6 col-md-6 col-sm-6 contact'>
  {{#if contact.first_name}}
    {{contact.first_name}}
  {{else}}
    Noname
  {{/if}}
  {{#if contact.last_name}}
    {{contact.last_name}}
  {{else}}
    Noname
  {{/if}}
  <ul>
    {{#if contact.job_title}}
      <li>Job Title: <b>{{contact.job_title}}</b></li>
    {{else}}
      <li><b>Seeking Employment</b></li>
    {{/if}}
    {{#if contact.phone_number}}
      <li>Phone Number: <b>{{contact.phone_number}}</b></li>
    {{else}}
      <li><b>Phone inactive</b></li>
    {{/if}}
    {{#if contact.email}}
      <li>Email: <b>{{contact.email}}</b></li>
    {{else}}
      <li><b>Email inactive</b></li>
    {{/if}}
  </ul>

</div>
{{/link-to}}

{{/each}}

{{else}}

<p>Your search does not match any contacts</p>

{{/if}}

<p class='col-lg-12 col-md-12 col-sm-12 text-center'>
  {{#link-to 'contacts'}}Return to Contacts{{/link-to}}
</p>
